// data:
// initial amount
// annual contribution
// expected return
// duration

function calculateInvestment(data: number[]) {} // => result[]

function printResults(results: number[]) {
   // print output the result data
}

const results = calculateInvestment([1, 2, 3]);

printResults(results);
